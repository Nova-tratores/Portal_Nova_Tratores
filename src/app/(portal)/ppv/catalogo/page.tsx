// /ppv/catalogo — mesma tela do PPV, abrindo direto na aba Catálogo.
// A página do PPV detecta o pathname e já sobe com a aba certa (e mantém a URL
// sincronizada quando você troca de aba), então dá pra linkar/favoritar o catálogo.
export { default } from "../page";
